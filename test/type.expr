letrec (
  # type id map
  # Void -> 0
  # Integer -> 1
  # String -> 2
  # Closure -> 3
  # Continuation -> 4
  a = (void)
  b = 0
  c = ""
  d = lambda () { (void) }
  e = (callcc lambda (k) { (k k) })
) {
  [
    (put (type a) "\n")
    (put (type b) "\n")
    (put (type c) "\n")
    (put (type d) "\n")
    (put (type e) "\n")
  ]
}
