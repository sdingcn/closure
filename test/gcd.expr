letrec (
  # greatest common divisor of nonnegative integers
  gcd = lambda (a b) {
    if (.< b 1) then a
    else (gcd b (.% a b))
  }
) {
  (.put (gcd (.strint (.getline)) (.strint (.getline))) "\n")
}
