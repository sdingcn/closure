letrec (
  reverse = lambda (s) {
    if (lt (strlen s) 1) then s
    else letrec (
      h = (strslice s 0 1)
      t = (strslice s 1 (strlen s))
      rt = (reverse t)
    ) {
      (strcat rt h)
    }
  }
) {
  letrec (
    line = (getline)
  ) {
    (put (reverse line) "\n")
  }
}
